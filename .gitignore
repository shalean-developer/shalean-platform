# Dependencies and tool caches at repo root (e.g. Vitest under node_modules/.vite)
node_modules/

# Local env
.env
.env.local
.env.*.local

# Supabase CLI local cache (version pins, etc.)
supabase/.temp/

# Local audit / scratch outputs
route-audit-temp.json
*-audit-temp.json
*-temp.json

# Lighthouse / local tool caches
.tmp-lh/

# Local SEO data exports (keep *.example.json)
apps/web/tmp/gsc-rows.json

supabase/.branches/
