# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v53.0.0/ before writing any code.

Follow `docs/customer-mobile-prd.md` and the approved Customer Mobile blueprint.
Implement **one milestone at a time**. Reuse `@shalean/*` and existing `/api/*` routes.
