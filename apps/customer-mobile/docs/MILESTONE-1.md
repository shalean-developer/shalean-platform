# Milestone 1 release notes — Customer Mobile scaffold

## What shipped

- New Expo app at `apps/customer-mobile`
- Monorepo Metro + NativeWind + `@shalean/*` wiring
- Expo Router shells: bootstrap, `(auth)`, `(tabs)`
- Health check against existing `GET /api/health`
- Separate bundle IDs / scheme from Cleaner app

## Not in this milestone

- Authentication
- `packages/mobile-ui`
- Booking, payments, push
