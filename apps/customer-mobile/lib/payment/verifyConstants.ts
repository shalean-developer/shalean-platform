/** Mirror web `/booking/success` verify loop + persist-pending status poll. */
export const VERIFY_MAX_ATTEMPTS = 3;
export const VERIFY_RETRY_DELAY_MS = 1500;

export const STATUS_POLL_MAX_ATTEMPTS = 10;
export const STATUS_POLL_DELAY_MS = 2000;
