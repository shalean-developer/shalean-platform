/**
 * Batch SEO blog generator — runs the programmatic matrix importer (`generate-programmatic-posts.ts`).
 *
 * Usage (from apps/web):
 *   npx tsx scripts/generateBlogPosts.ts
 *   npx tsx scripts/generateBlogPosts.ts --dry-run --limit=80
 */

import "./generate-programmatic-posts";
