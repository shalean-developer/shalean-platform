{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "node -e \"process.exit(['main','staging'].includes(process.env.VERCEL_GIT_COMMIT_REF) ? 1 : 0)\"",
  "installCommand": "npm ci"
}
