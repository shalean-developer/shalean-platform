/** Booking-funnel suburb hints (see `lib/booking/bookingFlowLocationCatalog.ts`). */
export {
  BOOKING_FLOW_LOCATION_HINTS,
  LOCATIONS,
  findLocationByName,
  findLocationBySlug,
  normalizeLocationSlugParam,
  type BookingFlowLocationArea,
} from "@/lib/booking/bookingFlowLocationCatalog";
