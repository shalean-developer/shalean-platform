/**
 * Unified booking quote engine version — binds price + duration + workload in one signed result.
 * Bump when signature payload or duration/price coupling rules change.
 */
export const BOOKING_QUOTE_ENGINE_VERSION = 1 as const;
