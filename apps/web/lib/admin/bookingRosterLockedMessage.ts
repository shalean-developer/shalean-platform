/** Shown when roster/team edits are blocked because line earnings are finalized. */
export const BOOKING_ROSTER_LOCKED_HINT =
  "This job's cleaner line earnings are finalized. To change the team or roster, reopen earnings for this booking first.";

/** Shown alongside the lock hint when admin force-assign is available. */
export const BOOKING_ROSTER_LOCKED_FORCE_HINT =
  "Or enable Force assign to reopen cleaner line earnings and assign the team anyway.";

export const ROSTER_FINALIZED_CODE = "roster_finalized" as const;
