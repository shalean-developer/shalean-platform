/**
 * Algorithm / lock-schema version (surge, charm, signature shape).
 * Rate changes live in Supabase — new rows in `pricing_versions`, not this constant.
 */
export const PRICING_ENGINE_ALGORITHM_VERSION = 6 as const;
