/** Shared primary booking CTA — marketing homepage visual consistency. */
export const marketingPrimaryCtaClassName =
  "inline-flex items-center justify-center bg-blue-600 text-white px-6 py-3 rounded-full font-semibold hover:bg-blue-700 transition";

/** Icon-only companion to primary booking CTA (same colors, compact tap target). */
export const marketingPrimaryCtaIconClassName =
  "inline-flex h-12 w-12 shrink-0 items-center justify-center bg-blue-600 text-white rounded-full font-semibold hover:bg-blue-700 transition";
