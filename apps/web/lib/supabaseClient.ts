/**
 * Browser Supabase client (anon key). Prefer `getSupabaseClient()` in new code;
 * legacy modules use `getSupabaseBrowser()` from `@/lib/supabase/browser`.
 */
export { getSupabaseBrowser as getSupabaseClient } from "@/lib/supabase/browser";
