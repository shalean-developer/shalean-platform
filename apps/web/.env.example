# Admin dashboard + /api/admin/* — must match the Supabase Auth email you sign in with (server-side only).
ADMIN_EMAILS=you@yourdomain.com

# Cleaner APIs: allow spoofable `x-cleaner-id` when no Bearer (default false — use only during rollout).
# CLEANER_AUTH_ALLOW_LEGACY_HEADER=true
