/** Segment config must be literals in this file — Next.js does not analyze re-exports for `runtime` / `dynamic`. */
export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export { POST } from "../assign/route";
